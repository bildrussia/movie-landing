# CinemaWave — киносайт-лендинг

Одностраничный сайт-лендинг киноресурса, выполненный как **итоговый проект**
по курсу «HTML / CSS / JavaScript». Без фреймворков и сборщиков —
чистый ванильный стек, готовый к публикации на GitHub Pages.

**Живой сайт:** https://bildrussia.github.io/movie-landing/

---

## Возможности

- **Семантическая HTML-структура**: `header`, `nav`, `main`, `section`, `article`, `footer`, `form`.
- **Адаптивная вёрстка** на **CSS Grid** и **Flexbox** (десктоп / планшет / телефон).
- **Светлая и тёмная темы** с переключением и **сохранением выбора в `localStorage`** (учитывает системные настройки).
- **Каталог фильмов с реальными данными Кинопоиска**:
  - Постеры, рейтинги, год выпуска и жанры — всё из API.
  - Фильтры по жанрам (боевик, драма, фантастика, комедия, триллер).
  - Кнопка **«Показать ещё»** — динамически добавляет карточки порциями по 6.
  - Счётчик «показано X из Y».
- **Форма обратной связи** с валидацией:
  - Имя, email (regex), телефон, сообщение, согласие.
  - На `submit` данные выводятся в `console.log` объектом.
  - Подсветка ошибок, live-валидация, успешный экран после отправки.
- **Мобильное меню** (бургер), плавная прокрутка по якорям.
- Анимации: плавающие постеры в hero, hover-эффекты, появление карточек.
- Доступность: `aria-label`, skip-link, поддержка клавиатуры, `prefers-reduced-motion`.

---

## Технологии

| Слой    | Что используется                                              |
|---------|---------------------------------------------------------------|
| Разметка| HTML5 (семантические теги)                                    |
| Стили   | CSS3 — Custom Properties, Grid, Flexbox, `@media`, анимации   |
| Логика  | JavaScript ES6+ (без зависимостей)                            |
| Шрифты  | Google Fonts — **Fraunces** (заголовки) + **Manrope** (текст) |
| API     | Кинопоиск Unofficial API (постеры, рейтинги, метаданные)      |
| Хостинг | GitHub Pages                                                  |

---

## Структура проекта

```
movie-landing/
├── index.html      # Разметка сайта
├── styles.css      # Все стили + темы + адаптив
├── script.js       # Тема, каталог, фильтры, форма, меню, API
├── README.md       # Этот файл
└── screenshots/    # Скриншоты для README
```

---

## Запуск локально

Сайт полностью статический — сборка не нужна.

```bash
# Способ 1: открыть index.html в браузере двойным кликом
open index.html        # macOS
xdg-open index.html    # Linux
start index.html       # Windows

# Способ 2: поднять локальный сервер (рекомендуется)
python3 -m http.server 8000
# затем открыть http://localhost:8000
```

---

## Соответствие критериям задания

| Критерий                                                      | Реализация |
|---------------------------------------------------------------|------------|
| Семантическая HTML-структура (`header`, `main`, `footer`)     | `index.html` |
| Шапка с навигацией                                            | `.header`, `.nav` |
| Блок с карточками фильмов                                     | `.movies` (24 фильма с Кинопоиска) |
| Форма обратной связи                                          | `#contactForm` |
| Использование Flexbox/Grid для сетки карточек                 | CSS Grid `.movies`, `.features__grid` |
| Кнопка «Показать ещё» (динамически добавляет карточки)        | `#loadMore` + `appendNext()` |
| Переключение темы с сохранением в `localStorage`              | `#themeToggle` + ключ `cinemawave-theme` |
| Форма выводит данные в `console.log` при отправке             | `console.log("[CinemaWave] Данные формы:", data)` |
| Валидация полей формы (включая email)                         | regex-проверки + live-валидация |

---

## Скриншоты

### Главный экран

| Светлая тема                                          | Тёмная тема                                           |
|--------------------------------------------------------|-------------------------------------------------------|
| ![Light hero](screenshots/desktop-light-hero.png)      | ![Dark hero](screenshots/desktop-dark-hero.png)      |

### Каталог фильмов с постерами Кинопоиска

| Каталог (светлая тема)                                       | Каталог (тёмная тема)                                          |
|---------------------------------------------------------------|-----------------------------------------------------------------|
| ![Light catalog](screenshots/desktop-light-catalog.png)       | ![Dark catalog](screenshots/desktop-dark-catalog.png)           |

### Форма обратной связи

| Светлая тема                                       | Тёмная тема                                        |
|----------------------------------------------------|----------------------------------------------------|
| ![Form light](screenshots/desktop-light-form.png)  | ![Form dark](screenshots/desktop-dark-form.png)    |

### Мобильная версия

| Hero                                                | Каталог                                              |
|-----------------------------------------------------|-------------------------------------------------------|
| ![Mobile hero](screenshots/mobile-hero.png)         | ![Mobile catalog](screenshots/mobile-catalog.png)     |

---

## Лицензия

MIT — используй свободно как пример для учебных проектов.
