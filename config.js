/* =========================================================
   CinemaWave — конфигурация
   ---------------------------------------------------------
   Если хочешь подгрузить реальные постеры с Кинопоиска:
     1) Получи бесплатный ключ на https://kinopoiskapiunofficial.tech/
     2) Создай рядом файл `config.local.js` (не коммитится) с содержимым:

        window.CINEMAWAVE_CONFIG.KINOPOISK_API_KEY = "ваш-ключ";

     3) В index.html подключи его после config.js.

   Без ключа сайт показывает заранее подготовленный набор
   фильмов — все функции (фильтры, «Показать ещё») работают.
   ========================================================= */

window.CINEMAWAVE_CONFIG = {
  // ВНИМАНИЕ: ключ не хранится в репозитории.
  // На GitHub Pages по умолчанию используется fallback-набор фильмов.
  // Локально подставь ключ через config.local.js (см. инструкцию выше).
  KINOPOISK_API_KEY: "",
  KINOPOISK_API_URL: "https://kinopoiskapiunofficial.tech/api/v2.2",
  // false — сразу показать локальные карточки, true — пробовать API
  USE_API: true,
  // сколько фильмов запрашивать у API
  API_LIMIT: 18,
};
